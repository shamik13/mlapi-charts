apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlapi-dep
  namespace: {{ .Values.app.env  }}
  labels:
  {{- include "obj.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.app.replicas }}
  selector:
    matchLabels:
      app: mlapi
  template:
    metadata:
      labels:
        {{- include "mlapi.labels" . | nindent 8 }}
    spec:
      containers:
        - name: mlapi
          image: mlapi:{{ .Chart.AppVersion }}
          imagePullPolicy: IfNotPresent
          resources:
           {{- toYaml .Values.app.computerc | nindent 12 }}
          envFrom:
          - configMapRef:
              name: mlapi-config